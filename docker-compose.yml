services:
  salty-server:
    container_name: salty-server
    build: .
    ports:
      - "8080:5010"
    volumes:
      - ./data:/usr/src/app/data
      - ./logs:/usr/src/app/logs
    environment:
      - NODE_ENV=production
      - PORT=5010
    restart: unless-stopped 