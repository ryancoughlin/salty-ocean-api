services:
  salty-server:
    build: .
    ports:
      - "5010:5010"
    volumes:
      - ./data:/usr/src/app/data
      - ./logs:/usr/src/app/logs
    environment:
      - NODE_ENV=production
      - PORT=5010
    restart: unless-stopped 